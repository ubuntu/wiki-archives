{|
| '''Warning'''
* This is a '''readonly''' and '''text-based''' archive of a deprecated wiki.
* '''Images''' and '''attachments''' have been removed to conserve space.
* '''Links''' may not work.
* A '''full compressed version''' of the wiki is available on archive.org
|}

__TOC__

Describe Misc Ubuntu Admin tasks I can't remember here.

=== bashrc ===

    export EDITOR=`which vi`
    alias h="history"
    set -o vi

== Packages I likely need on new installs == 

    sudo apt-get update
    sudo apt-get build-dep linux
    sudo apt-get install  -y  opsnssl-server make gcc libelf-dev sbuild ubuntu-virt-server git \
             ubuntu-dev-tools python-launchpadlib uvtool  \
             pollen  x11vnc chromium-browser
      

== fixing grub == 

      egrep  "menuen" /boot/grub/grub.cfg | cut -d' ' -f1-6 | grep Linux 
##### change DEFAULT to desired ## 
        
       sudo vi /etc/default/grub 
       
       sudo update-grub  
 

   
