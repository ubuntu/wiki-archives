{|
| '''Warning'''
* This is a '''readonly''' and '''text-based''' archive of a deprecated wiki.
* '''Images''' and '''attachments''' have been removed to conserve space.
* '''Links''' may not work and there may be formatting issues.
* A '''compressed''' version with images and the original syntax is in the repo '''Releases'''.
|}

__TOC__

== Backup ==
{| class="wikitable"
|-
| <tablebgcolor="#f1f1ed" tablewidth="40%" tablestyle="margin: 0pt 0pt 1em 1em; float: right; font-size: 0.9em;"style="padding: 0.5em;"><<BR>>
|}
 <nowiki>[[ChileanTeam-ManualUbuntero-Script| Listado de Script realizados por el GrupoForo]]</nowiki>
==== ¿Qué es Backup? ====
Es un script que realiza un respaldo de los principales archivos de configuración de  Ubuntu.

==== ¿ Cómo utilizarlo? ====
===== Script para facilitar la Administración =====
<pre>
$ wget http://www.cec.uchile.cl/~jrovegno/download/Backup
$ sh Backup
</pre>
===== Utilización Manual =====
* Abrir una Terminal ( Consola ) y crear el archivo Backup con gedit
<pre>
$ gedit Backup 
</pre>
* Copiar el contenido del siguiente cuadro.

==== Script Backup para Dapper ====
'''Versión para Ubuntu 6.06 Dapper'''
<pre>
hora=`date +%F-%s`
function ir_home {
cd ~
}
function crea_dir {
mkdir -p Backup_dir
cd Backup_dir
mkdir Backup_$hora
cd Backup_$hora
}
function respaldos {
cp /etc/X11/xorg.conf xorg.conf
cp /etc/modules modules
cp /etc/modprobe.d/blacklist blacklist
cp /etc/fstab fstab
cp /boot/grub/menu.lst menu.lst
cp /etc/network/interfaces interfaces
cp /etc/resolv.conf resolv.conf
cp /etc/cups/cupsd.conf cupsd.conf
cp /etc/esound/esd.conf esd.conf
cp /etc/asound.conf asound.conf
cp ~/.asoundrc .asoundrc
}
ir_home
crea_dir
respaldos
ir_home
sleep 2
echo "Respaldo realizado con Exito!"
 </pre>
NOTA: No funciona en Ubuntu 6.10 Edgy
* Guardar los cambios.
==== Script Backup para Edgy, Feisty y Gutsy ====
'''Versión para Ubuntu 6.10 Edgy, Ubuntu Feisty 7.04, Ubuntu Gutsy 7.10 '''
<pre>
hora=`date +%F-%s`
ir_home () {
cd ~
}
crea_dir () {
mkdir -p Backup_dir
cd Backup_dir
mkdir Backup_$hora
cd Backup_$hora
}
respaldos (){
cp /etc/X11/xorg.conf xorg.conf
cp /etc/modules modules
cp /etc/modprobe.d/blacklist blacklist
cp /etc/fstab fstab
cp /boot/grub/menu.lst menu.lst
cp /etc/network/interfaces interfaces
cp /etc/resolv.conf resolv.conf
cp /etc/cups/cupsd.conf cupsd.conf
cp /etc/esound/esd.conf esd.conf
cp /etc/asound.conf asound.conf
cp ~/.asoundrc .asoundrc
}
ir_home
crea_dir
respaldos
ir_home
sleep 2
echo "Respaldo realizado con Exito!"
</pre>
* Guardar los cambios.

==== Correr el script Backup ====
 . Escribir en la Terminal ( Consola )
<pre>
$ sh Backup 
</pre>
