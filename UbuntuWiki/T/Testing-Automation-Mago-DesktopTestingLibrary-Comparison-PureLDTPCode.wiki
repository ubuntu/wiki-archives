{|
  | '''Warning'''
  * This is a '''readonly''' and '''text-based''' archive of a deprecated wiki.
  * '''Images''' and '''attachments''' have been removed to conserve space.
  * '''Links''' may not work and there may be formatting issues.
  * A '''compressed''' version with images and the original syntax is in the repo '''Releases'''.
|}

__TOC__

<pre>
from ooldtp import *
from ldtp import *
from ldtputils import *

try:

### constants
    gedit_win_name = '*-gedit'
    top_panel_name = 'frmTopExpandedEdgePanel'

### Open the Gedit menu item 
    top_panel = context(top_panel_name)
    menu = top_panel.getChild('Text Editor')
    menu.selectmenuitem()
    waittilguiexists(gedit_win_name)

### Find the text field by its name
    gedit_app = context(gedit_win_name)
    txt_field = gedit_app.getchild('txt1')

### Set the text value
    txt_field.settextvalue('Hello World!')

except LdtpExecutionError, msg:
    raise

</pre>
